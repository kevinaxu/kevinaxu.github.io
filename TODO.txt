Front End
[] sidebar disappear on scroll (keep the "table of contents" bar)
[] homepage should display first few images of each story
[] map view slat (ex Alaska trip should "follow" the map as the scroll continues)

Misc.
[] setup google analytics
[] update Expose script to embed google analytics into every index.html page
[x] setup ssh access
[x] link expose here (and add .gitignore for expose)

Configs
[] combine per-image configs into global config
[] config should handle ordering of images, not file name

Performance
[] optimize images (remove option for higher quality images)
